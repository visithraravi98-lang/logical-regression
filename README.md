# logical-regression
This project implements logistic regression using only Numpy, without high level machine learning libraries.It demonstrates the theory behind logistic regressio,includig the sigmoid function,cost function,gradient descent, and model evaluation.This model is tested on a synthetic dataset and compared to scikit-learns implementation.
